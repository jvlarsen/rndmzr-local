

const randomize = (selectedEvent, selectedPlayer) => {
  console.log(selectedEvent + ' - ' + selectedPlayer + ' printed from Connector.');
  var result = [{'status':'status0','value': 3}, {'status':'status1', 'value':2}, {'status':'status2', 'value':8}];
  return result;
}

export default {
  randomize
}
